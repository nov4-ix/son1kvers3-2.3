# 🔐 RAILWAY SECRETS - TEMPLATE
# Copia este archivo y renómbralo a .railway-secrets.env
# NO COMMITEES el archivo .env con valores reales

# ============================================
# CRÍTICO - DEBES COMPLETAR ESTOS
# ============================================

# 1. SUNO TOKENS (separados por comas, sin espacios)
# Obtenerlos de: https://suno.com (DevTools → Cookies → clerk_token)
SUNO_TOKENS=token1_aqui,token2_aqui,token3_aqui

# 2. JWT SECRET (mínimo 32 caracteres)
# Generar con: openssl rand -base64 32
JWT_SECRET=tu_jwt_secret_super_seguro_minimo_32_caracteres_aqui

# 3. TOKEN ENCRYPTION KEY (mínimo 32 caracteres)
# Generar con: openssl rand -base64 32
TOKEN_ENCRYPTION_KEY=tu_encryption_key_super_seguro_minimo_32_caracteres

# ============================================
# AUTO-CONFIGURADAS (no editar)
# ============================================

# Railway auto-conecta estos valores
DATABASE_URL=${Postgres.DATABASE_URL}
REDIS_URL=${Redis.REDIS_URL}

# Base
NODE_ENV=production
PORT=3000

# APIs Externas (ya configuradas)
GENERATION_API_URL=https://ai.imgkits.com/suno
NEURAL_ENGINE_API_URL=https://ai.imgkits.com/suno
COVER_API_URL=https://usa.imgkits.com/node-api/suno
GENERATION_POLLING_URL=https://usa.imgkits.com/node-api/suno
NEURAL_ENGINE_POLLING_URL=https://usa.imgkits.com/node-api/suno

# CORS (actualizar después con tu dominio Vercel)
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000

# ============================================
# OPCIONAL pero recomendado
# ============================================

# Health check interval
HEALTH_CHECK_INTERVAL=300000

# Token pool refresh
TOKEN_POOL_REFRESH_INTERVAL=60000

